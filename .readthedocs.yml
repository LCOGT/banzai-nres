version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv sync --extra docs --extra cpu
    - uv run sphinx-build -b html docs $READTHEDOCS_OUTPUT/html

formats: []
